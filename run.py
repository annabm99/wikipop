from market import app # call app variable from the init file.
    # market is the subdirectory where the app is saved

if __name__=='__main__': # Command to run the app
    app.run(debug=True) # True so we can see edits real-time